{
  "name": "jogo-multiplayer",
  "version": "1.0.0",
  "description": "Multiplayer Game with React Frontend and Node.js Backend",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build && cd ..",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
