services:
  - type: web
    name: jogo-multiplayer
    runtime: node
    plan: free
    buildCommand: bash build.sh
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
